# Leave unset to use the vite dev proxy (recommended: the C# API has no CORS).
# VITE_API_URL=/api
# VITE_GRAPH_HUB_URL=/hubs/graph

# Where the vite proxy forwards to. The C# public API defaults to :5107.
VITE_PROXY_TARGET=http://localhost:5107
