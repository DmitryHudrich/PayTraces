# Frontend talks to the C# public API (Ledgerscope.Accounts) through the vite
# dev proxy by default, so no VITE_API_URL is needed locally.
# VITE_API_URL=/api
# VITE_GRAPH_HUB_URL=/hubs/graph

# Backend the dev proxy forwards to (REST + SignalR hub).
VITE_PROXY_TARGET=http://localhost:5107
